services:
  api:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
      - PORT=3000
      - GENERATOR_URL=http://localhost:5000
    volumes:
      - ./src:/app/src
    depends_on:
      - model-generator
